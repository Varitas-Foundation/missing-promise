# LLM API access (OpenRouter, HTTP client) - only needed to re-run classification
httpx>=0.27
python-dotenv>=1.0

# Stability comparison, ablation comparison, and inferential statistics
# (scripts/stability_comparison.py, scripts/ablation_comparison.py, scripts/analyze_inferential.py)
scipy>=1.12
